1. Adding sleep in c++ thread -

// #include <chrono>
// std::this_thread::sleep_for(std::chrono::milliseconds(1000));

2. Initializing thread array

// std::thread t[x];
// for(int i = 0; i < x; i++) { t[i] = std::thread(func, args...); }
